-module(eratos).


gen(Proc) -> gen(Proc,2).
gen(Proc, N) ->
     %% Generate the initial list of numbers. Ensure we have not crosssed Max
    end.  

filtloop -> %% Fix the correct number of arguments 
     %% filter out multiples of current prime to send to next sieve
     end.

sieve() ->
     %% receive a prime, spawn the next sieve and call filtloop
    end.

main() ->
    %% start with the first sieve
